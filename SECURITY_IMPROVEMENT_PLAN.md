# セキュリティリスク最小化計画書

**作成日**: 2025年2月19日  
**目的**: 既知のセキュリティリスクを体系的に解消し、実用上「リスクゼロ」に近づける  
**参照**: SECURITY_AUDIT_REPORT.md, SECURITY_FIX_PLAN.md

---

## 0. 前提とスコープ

- **「リスクゼロ」の定義**: すべての既知・想定される攻撃ベクトルに対して対策を講じ、残存リスクを許容可能な水準まで低減する
- ゼロデイ脆弱性や人的ミスによる漏洩は完全には防げないため、監視・検知・運用面の対策も含める
- 本計画は静的解析・手動確認に基づく。ペネトレーションテスト・外部スキャンは別途実施推奨

---

## 1. 実施済みの対策（ベースライン）

| 項目 | 状態 |
|------|------|
| セキュリティヘッダー | X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy 設定済み |
| Middleware 認証 | 認証必須 API を網羅（/api/analyze-meal 含む） |
| allowDangerousEmailAccountLinking | `false` |
| Server Actions bodySizeLimit | 2MB |
| 環境変数 | サーバー用は `getServerEnv()` 経由に統一（LINE, VAPID, CRON 含む） |
| IDOR 対策 | 全 API が `session.userId` で DB クエリ、クライアント送信を信用しない |
| 機密情報 | 既往歴・服薬は DB から取得、POST body で受けない |
| LINE Webhook | HMAC-SHA256 署名検証 |
| Cron API | X-Cron-Secret で保護 |
| XSS | dangerouslySetInnerHTML / eval 未使用 |
| SQL インジェクション | Prisma パラメータ化済み |
| minimatch ReDoS | package.json overrides で 10.2.1 に強制、高リスク解消済み |

---

## 2. 残存リスクと対応計画

### フェーズ 1: 即時対応（1〜2週間）

| # | リスク | 深刻度 | 対応内容 | 工数 | 完了基準 |
|---|--------|--------|----------|------|----------|
| 1.1 | **ajv ReDoS**（開発時） | 中 | ESLint 系の更新待ち。現状は開発時のみ影響、本番非影響。npm の週次確認を運用に追加 | 小 | 監視継続、将来の eslint-config-next 更新で解消 |
| 1.2 | **process.env の例外** | 低 | `lib/prisma.ts` の `NODE_ENV`、`instrumentation.ts` のビルド判定を `getServerEnv().NODE_ENV` 等に統一するか検討（任意） | 小 | プロジェクトルールとの整合性確認 |
| 1.3 | **CRON_SECRET 強度** | 低 | 本番環境で 32 文字以上のランダム文字列であることをドキュメント化。デプロイチェックリストに追加 | 極小 | README または DEPLOY.md に記載 |

---

### フェーズ 2: 中期対応（1〜2ヶ月）

| # | リスク | 深刻度 | 対応内容 | 工数 | 完了基準 |
|---|--------|--------|----------|------|----------|
| 2.1 | **Content-Security-Policy 未設定** | 中 | 段階的に CSP を導入。まず `default-src 'self'` で開始し、外部スクリプト（LINE 等）を `script-src` に許可リスト形式で追加 | 中 | CSP ヘッダーが有効。レポートのみモードで検証後、enforce に切り替え |
| 2.2 | **typescript.ignoreBuildErrors: true** | 中 | 型エラーを順次修正し、`ignoreBuildErrors` を `false` に戻す | 大 | ビルドが型チェック付きで成功 |
| 2.3 | **レートリミット未実装** | 中 | 認証・AI 相談・LINE 連携・Cron 等の重要 API にレートリミットを導入（例: `@upstash/ratelimit` または Cloud Run の制限） | 中 | 主要 API が制限内で動作。超過時 429 を返す |

---

### フェーズ 3: 長期対応（3ヶ月〜）

| # | リスク | 深刻度 | 対応内容 | 工数 | 完了基準 |
|---|--------|--------|----------|------|----------|
| 3.1 | **ログの機密情報漏洩** | 低 | 本番用ログミドルウェアを導入。スタックトレース・シークレット・個人情報をマスキング | 中 | 本番ログにシークレットが含まれないことを確認 |
| 3.2 | **HSTS ヘッダー** | 低 | HTTPS 強制のため `Strict-Transport-Security` を追加（CDN/ロードバランサーで設定する場合も可） | 小 | 本番で HSTS が有効 |
| 3.3 | **依存関係の継続的監視** | 低 | Dependabot / Renovate による自動 PR、または週次の `npm audit` 実行 | 小 | 脆弱性検出から 1 週間以内にレビュー |

---

### フェーズ 4: 運用・検知・回復

| # | リスク | 深刻度 | 対応内容 | 工数 | 完了基準 |
|---|--------|--------|----------|------|----------|
| 4.1 | **インシデント検知** | 中 | エラーログ・異常アクセス検知の仕組み（Cloud Logging アラート等） | 中 | 異常時に通知される |
| 4.2 | **インシデント対応手順** | 低 | INCIDENT_RESPONSE.md を作成。連絡先・エスカレーション・復旧手順を記載 | 小 | ドキュメント作成済み |
| 4.3 | **シークレットローテーション** | 低 | AUTH_SECRET、DB 接続、API キー等の定期的なローテーション手順を文書化 | 小 | 手順書が存在する |

---

## 3. 追加のハードニング案（オプション）

| 項目 | 内容 |
|------|------|
| CORS の明示的設定 | 許可オリジンをホワイトリストで制限 |
| 入力バリデーション | Zod 等で API リクエスト body をスキーマ検証 |
| サブリソース整合性 (SRI) | 外部 CDN のスクリプト利用時に integrity 属性を付与 |
| セッション有効期限 | 現在 7 日。機密性に応じて短縮を検討 |

---

## 4. 実行チェックリスト

### フェーズ 1

- [ ] 1.1 ajv: npm の定期確認運用を確立
- [ ] 1.2 process.env: prisma / instrumentation の getServerEnv 統一を検討
- [ ] 1.3 CRON_SECRET: 強度要件をドキュメントに記載

### フェーズ 2

- [ ] 2.1 CSP: 報告のみモードで導入、動作確認後 enforce
- [ ] 2.2 ignoreBuildErrors: 型エラー修正後に false に変更
- [ ] 2.3 レートリミット: 主要 API に導入

### フェーズ 3

- [ ] 3.1 ログマスキング: 本番ログの検証
- [ ] 3.2 HSTS: 本番環境で有効化
- [ ] 3.3 依存関係: Dependabot 等の設定

### フェーズ 4

- [ ] 4.1 アラート: Cloud Logging 等の設定
- [ ] 4.2 INCIDENT_RESPONSE.md 作成
- [ ] 4.3 シークレットローテーション手順の文書化

---

## 5. 検証方法

| 検証種別 | 実施内容 |
|----------|----------|
| ビルド | `npm run build` が成功すること |
| テスト | `npm test` が全件成功すること |
| 脆弱性スキャン | `npm audit` で高・重大が 0 であること |
| 静的解析 | ESLint がエラーなく通ること |
| 手動確認 | 認証・認可・LINE/Cron の動作確認 |

---

## 6. 再評価のタイミング

- 各フェーズ完了時
- 新規外部連携（LINE 以外）の導入時
- 認証方式・権限モデルの変更時
- 重大な依存関係脆弱性の公表時（CVE 等）

---

*本計画書は SECURITY_AUDIT_REPORT.md および現状のコードベースに基づいて作成した。実装時はプロジェクトの .cursor/rules に従うこと。*
